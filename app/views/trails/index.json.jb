@trails.map do |trail|
  {
    id: trail.id,
    name: trail.name,
    description: trail.description,
    length: trail.length,
    image_url: trail.image_url,
    conditions: trail.conditions,
    fast_facts: trail.fast_facts 
  }
end
